<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% if title %}{{ title }} | {% endif %}{{ site.title }}</title>
    <meta name="description" content="{{ description | default: site.description | strip_html | strip_newlines | truncate: 160 }}">
    <link rel="stylesheet" href="{{ site.base_path }}/assets/css/site.css?v={{ site.asset_version }}">
  </head>
  <body>
    <div class="site-shell">
      <header class="site-header">
        <a class="brand" href="{{ site.base_path }}/">{{ site.title }}</a>
        <nav class="site-nav" aria-label="Primary">
          <a href="{{ site.base_path }}/">Home</a>
          <a href="{{ site.base_path }}/blog/">Blog</a>
          <a href="{{ site.base_path }}/apps/">Apps</a>
        </nav>
      </header>

      <main class="site-main">
        {{ content }}
      </main>

      <footer class="site-footer">
        <p>{{ site.description }}</p>
      </footer>
    </div>
  </body>
</html>
